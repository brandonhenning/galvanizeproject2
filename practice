function countBs(word) {
  var numberofBs = 0;
  for (var i = 0; i < word.length; i++) {
    if (word.charAt(i) == "B") {
      numberofBs += 1;
    } else {}
  }
  console.log(`There are ${numberofBs} in this string!`);
}


countBs("thereisnobherewinkwink");


function countChar(word, hunted) {
    var numberOfHunted = 0;
    for (var i = 0; i < word.length; i++) {
      if (word.charAt(i) == hunted) {
        numberOfHunted += 1;
      } else {}
    }
    console.log(`There are ${numberOfHunted} in this string!`);
  }

}

countChar("I wonder if there are any is", "i");
